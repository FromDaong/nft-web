export const getUserTimeLine = (
  user: number,
  page: number,
  limit: number
) => {};

export const getHomeTimelineForUser = (
  user: string,
  page: number,
  limit: number
) => {};

export const getHomeTimelineForPlatform = (page: number, limit: number) => {};
