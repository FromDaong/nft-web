export const searchForNFTs = (
  filter: any,
  page: number,
  limit: number,
  moreLikeThis?: boolean
) => {};

export const searchForCreators = (
  filter: any,
  page: number,
  limit: number,
  moreLikeThis?: boolean
) => {};

export const searchForPosts = (
  filter: any,
  page: number,
  limit: number,
  moreLikeThis?: boolean
) => {};
